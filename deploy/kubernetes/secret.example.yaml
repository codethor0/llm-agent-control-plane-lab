# Reference only — copy pattern to your secret manager workflow.
# Do not apply this file with real credentials. Rename to Secret in-cluster via your platform process.
apiVersion: v1
kind: Secret
metadata:
  name: llm-control-plane-api-keys
  labels:
    app: llm-agent-control-plane
type: Opaque
stringData:
  api_keys.txt: |
    lab-fake-api-key-kubernetes-example-only
